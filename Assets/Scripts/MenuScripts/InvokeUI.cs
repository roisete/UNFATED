using UnityEngine;

public class InvokeUI : MonoBehaviour
{
    public static InvokeUI instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
